# GuiAgent 优化建议

本文档基于当前代码结构的快速审阅，先给出可立即执行的优化方向，不替代后续完整 review 报告。

## 1. 优先级最高的问题

### 1.1 收敛状态流定义
- 当前状态字段依赖 `AgentState` 这个“大字典”在各节点间隐式传递，`stop_flag`、`execution_status`、`retry_count`、`sub_flag`、`current_step_index` 共同驱动流程，语义容易漂移。
- 建议引入更明确的状态分层：
  - `TaskState`：任务级状态，如任务名、总步骤数、取消状态。
  - `StepState`：当前 step 的截图、动作、错误、重试次数。
  - `PlanState`：子任务分解结果、当前子任务索引、完成情况。
- 建议把路由条件统一成枚举，例如 `next_stage = CAPTURE / REASON / EXECUTE / RETRY / FINISH`，避免多个布尔值组合决定流向。

### 1.2 拆分 orchestration 与业务执行
- 目前 `agent_graph.py`、`app/server.py`、各节点都直接操作运行时细节，任务管理、图编排、学习接口、HTTP 接口耦合较重。
- 建议拆成 4 层：
  - `core/graph`：纯状态机与路由。
  - `core/services`：推理服务、规则服务、执行服务、日志服务。
  - `infra`：OpenAI client、文件存储、截图、鼠标键盘操作。
  - `app/api`：FastAPI 路由与 DTO。
- 目标是让 GUI/API 只依赖 `AgentRunner` 这样的应用服务，而不是直接依赖图构建细节。

### 1.3 清理重复实现
- `utils/utils.py` 中存在 `ComputerTools`、`StepPopup`、`extract_tool_calls`、`extract_template_request` 等重复实现；`utils/computer_tools.py`、`utils/popup.py`、`utils/parsers.py` 里也各有一份。
- 建议把公共能力拆成单一来源：
  - `utils/computer_tools.py`
  - `utils/popup.py`
  - `utils/parsers.py`
  - `utils/vision.py`
- `utils/utils.py` 应该被拆掉或降级为少量兼容导出层，否则后续维护成本会持续上升。

## 2. 状态流优化建议

### 2.1 明确“停止”与“完成”语义
- 现在“任务完成”“子任务完成”“取消”“错误终止”都在复用 `stop_flag`，建议拆开：
  - `is_cancelled`
  - `is_finished`
  - `finish_reason`
  - `step_outcome`
- `execution_status` 不建议同时表达节点执行结果和任务最终结果。

### 2.2 统一 step 计数规则
- 当前 `capture` 和 `continue_handler` 都会修改 `step_id`，容易导致 step 编号和真实截图/动作轮次不一致。
- 建议只在一个地方推进 step 序号，最好由 orchestrator 在进入新一轮感知时统一加一。

### 2.3 子任务流改为显式结果对象
- `sub_flag` 的语义不直观，建议改成显式结果：
  - `substep_status = CONTINUE_CURRENT / COMPLETE_AND_NEXT / COMPLETE_ALL`
- 这样 reasoning、execution、router 的职责会更清晰。

## 3. GUI 层与核心推理层解耦建议

### 3.1 保持前端只走 API
- 现有 dashboard 通过 HTTP API 调任务，这是合理方向，应继续保持。
- 不建议让前端直接感知 LangGraph 状态结构或节点名称。

### 3.2 服务端增加应用服务层
- 建议新增类似 `AgentApplicationService`：
  - `submit_task()`
  - `cancel_task()`
  - `get_task_status()`
  - `run_once()`
- FastAPI 路由只做参数校验和响应转换。

### 3.3 学习模块单独挂载
- `app/server.py` 当前同时承载任务 API 和 skill learning API，建议拆分为：
  - `app/routes/tasks.py`
  - `app/routes/skills.py`
  - `app/services/task_manager.py`
- 这样不会把任务执行主链路和技能学习管理耦在一个超大文件里。

## 4. 异常处理与日志建议

### 4.1 从 `print` 升级到结构化日志
- 建议统一使用 `logging`，并至少打出以下字段：
  - `task_id`
  - `step_id`
  - `node`
  - `event`
  - `status`
  - `error`
- 任务执行日志和 HTTP 访问日志建议分流。

### 4.2 异常分类
- 建议明确区分：
  - `RetryableError`
  - `FatalError`
  - `UserCancellation`
  - `ExternalServiceError`
- 错误处理节点应基于异常类型决定重试，而不是只看 `retry_count`。

### 4.3 保留失败上下文
- 失败时建议持久化：
  - 当前截图路径
  - 当前动作列表
  - 原始模型返回
  - 路由决策原因
- 这对 GUI agent 的问题定位非常关键。

## 5. 性能优化建议

### 5.1 缓存 RuleMatcher
- 当前 fast path 每一步都重新构造 `RuleMatcher` 并加载规则，建议在任务级或进程级缓存。

### 5.2 减少重复图片编码
- `build_messages()` 每步都会重新 base64 编码历史图片和当前图片，建议：
  - 对截图结果做缓存
  - 控制历史图片窗口
  - 尽量只传必要图像

### 5.3 控制阻塞等待
- `time.sleep()` 在 fast path、execution 中较多，会拖慢整体吞吐。
- 如果运行在服务端 worker 中，建议区分“必须等待 UI 稳定”和“调试期保守延迟”。

### 5.4 限制历史增长
- `history`、`messages`、日志数据如果长期增长，会拉高推理输入和内存占用。
- 建议设定窗口策略与摘要策略。

## 6. 推荐重构顺序

### 第一阶段：低风险收敛
- 清理重复工具实现，统一导入路径。
- 把 `print` 替换为统一 logger。
- 抽出 `TaskManager` 到独立模块。
- 给状态字段补充统一注释和枚举。

### 第二阶段：核心链路收敛
- 抽出 `RuleService`、`ReasoningService`、`ExecutionService`。
- 重构 `AgentState` 为分层对象。
- 统一 router 返回值与完成语义。

### 第三阶段：架构解耦
- 将 `app/server.py` 拆成多路由模块。
- 引入应用服务层，隔离 FastAPI 与 LangGraph。
- 为任务执行链路补充单测和状态流测试。

## 7. 建议马上做的三件事

1. 先消除重复模块与不一致导入，确保工具层只有一个可信实现。
2. 把状态流语义从“多个布尔值组合”收敛成明确枚举和结果对象。
3. 把 `app/server.py` 拆分，建立 API 层、任务管理层、核心执行层三层边界。

## 8. 下一步

如果你要，我下一步可以基于这个文档继续输出完整的 `review 报告`，按以下结构展开：
- 高优先级问题列表
- 状态流专项分析
- GUI/核心解耦分析
- 重复逻辑与抽象点
- 异常/日志覆盖分析
- 性能瓶颈分析
- 分阶段重构方案
